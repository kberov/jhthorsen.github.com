% layout 'default';
% title $basename;
<h1 class="page-header"><%= $basename %></h1>
%= include 'files/navbar'
<div class="image preview">
  %= link_to 'file.raw', begin
    %= image url_for('file.raw'), alt => $basename;
  % end
</div>
