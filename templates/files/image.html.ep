% layout 'default';
% title $basename;
<h1 class="page-header"><%= $basename %></h1>
%= include 'files/navbar'
<div class="image preview">
  %= link_to "$route_prefix.raw", begin
    %= image url_for("$route_prefix.raw"), alt => $basename;
  % end
</div>
