% layout 'default';
% title $basename;
<h1 class="page-header"><%= $basename %></h1>
%= include 'files/navbar'
<div class="image preview">
  %= link_to $raw_path, begin
    %= image $raw_path, alt => $basename;
  % end
</div>
